// String Format specifier --> "%s"
// in this program we will simply take input and output of string
#include <stdio.h>
int main() {
    char firstName[10];
    scanf("%s", firstName);// it will take input till space
    printf("%s", firstName);
    return 0;
}